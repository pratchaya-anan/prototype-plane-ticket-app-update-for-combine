import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'plane-ticket-app-update-for',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
